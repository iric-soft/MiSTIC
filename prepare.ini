[mistic:prepare]
# dataset preparation commands
transform = mistic transform
mst = mst -s clip -w pearson
graph-to-dot = mistic graph-to-dot -e neighbourhood
layout = sfdp

[app:main]
use = egg:mistic

cache.type = memory
cache.regions = mistic
cache.mistic.expire = 300

pyramid.reload_templates = true
pyramid.debug_authorization = false
pyramid.debug_notfound = false
pyramid.debug_routematch = false
pyramid.debug_templates = true
pyramid.default_locale_name = en
pyramid.includes = pyramid_debugtoolbar

mistic.data = %(here)s/prepare.yaml

mistic.basic.realm = Mistic bodymap instance
mistic.basic.auth = boucherg:{SHA}rKfKDp95yysIHCD50p7S8k0i8Go= leucegene:{SHA}ZaolFQfB7FQ69FkUmopPPi3uUII=
mistic.rsvg-convert = rsvg-convert

[server:main]
use = egg:Paste#http
host = 0.0.0.0
port = 8082 

# Begin logging configuration

[loggers]
keys = root, mistic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = console

[logger_mistic]
level = DEBUG
handlers =
qualname = mistic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s][%(threadName)s] %(message)s

# End logging configuration
